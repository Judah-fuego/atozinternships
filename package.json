{
  "name": "internships",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "scrape": "node scripts/scrape-internships.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "nuxt": "^4.5.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
