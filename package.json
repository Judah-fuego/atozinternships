{
  "name": "internships",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "scrape": "node scripts/scrape-internships.mjs",
    "scrape:summaries": "node scripts/scrape-internships.mjs --enrich-summaries",
    "test": "vitest run"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "nuxt": "^4.5.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
